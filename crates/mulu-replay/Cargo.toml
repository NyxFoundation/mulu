[package]
name = "mulu-replay"
description = "mulu: replay a concrete counterexample on a local EVM"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
ruint.workspace = true
# Pinned exactly: the version is recorded in every replay record, and
# REVM_VERSION in src/lib.rs must move with it (a test checks they agree).
revm = "=43.0.0"
